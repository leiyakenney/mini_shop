<h1>All Items</h1>

<% @items.each do |item| %>
  <h3> <%= item.name %> </h3>
  <p> <%= item.description %> </p>
  <p> <%= item.price %> </p>
  <p> <%= item.image %> </p>
  <p> <%= item.status %> </p>
  <p> <%= item.inventory %> </p>
  <p> <%= item.merchant %> </p>
<% end %>
